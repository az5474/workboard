# 줄바꿈을 저장소 안에서는 LF 하나로 통일한다.
# 이게 없으면 윈도우에서 받을 때마다 파일 전체가 바뀐 것처럼 보인다.
* text=auto eol=lf

# PowerShell 스크립트만 예외로 CRLF 를 유지한다.
# (UTF-8 BOM 은 파일 내용이라 git 이 그대로 보존한다. 지우면 PS 5.1 에서 한글이 깨진다.)
*.ps1 text eol=crlf
